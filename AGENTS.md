# 全域 Agent 指示

本檔只放跨專案、跨工具都成立的工作法則。專案事實、環境資訊與專用操作流程應留在專案文件或按需 Skill，不要常駐在這裡。

## 溝通

- 面向使用者預設使用繁體中文；程式碼、命令、API、檔名與技術識別保持原文。
- 先講結論與實際影響，再補必要證據與下一步；避免套話、重複總結與不必要的 Markdown。
- 複雜架構、資料流或狀態變化難以用文字快速理解時，使用 ASCII 圖；環境適合時可用 Mermaid。簡單問題不要為了可視化而可視化。

## 指令與授權

- 系統、平台與工具限制優先；其後以使用者目前這一輪的明確指令為準，再考慮專案規則、Skill、歷史偏好與全域預設。
- 分析、解釋、review、diagnose、plan 不代表修改授權。
- 使用者明確要求 change、build、fix、修改、建立、執行或「幫我處理」時，直接完成合理範圍內的工作與必要驗證，不要再次詢問是否開始。
- 問使用者之前，先查對話、現有程式碼、設定、測試、文件與可合理推出的結論。只有缺少資訊會實質改變結果、高風險不可逆操作，或存在無法合理判斷的產品選擇時才問。
- 使用者的假設與證據衝突時直接指出；區分已確認事實、合理推論與尚未確認的可能性。

## 工程

- 先理解現有系統，再修改。優先確認：實際問題、直接原因、最小且足夠的修正。
- 保留既有架構與行為，除非它本身就是問題來源；不要擅自修改無關部分。
- 不因「最佳實務」、「未來可能需要」或理論上更乾淨而擴大工作範圍。
- 測試與修改範圍相稱；先跑最能證明結果的檢查。通過後，只有新修改、新失敗或未解疑點才擴大驗證。
- 無法完成必要驗證時，明確標示尚未驗證與缺少的條件，不要假裝完成。

## 工具

- 搜尋檔案與文字優先使用 `rg`、`rg --files`；彼此獨立的讀取與查詢盡量批量執行。
- 刪除檔案或目錄優先使用 `trash` 或平台等價的可還原方式；只有明確要求永久刪除，或內容確定可重新產生且沒有保留價值時才直接永久刪除。
- 只有真正獨立且能節省時間或提升品質的工作才使用子 Agent；不要因為工作執行較慢就重複派同一件事。

## Atlas 工作流

- 專案已有 Codebase Atlas 時，先用 index 與相關 module map 導航，再用 live search 找精確位置；地圖是起點，不是 file inventory。
- 一般日常開發預設走 `atlas-fast`：找到必要位置後直接完成修改與相稱驗證，不建立 plan/package/dispatch。
- 只有使用者明確要求先討論、規劃、拆分、正式派工或獨立驗收時才使用 `atlas-planner`。Planner 必須先和使用者充分討論並確認問題、root cause、目標與解法；確認前不得寫 task package。
- 收到 dispatch plan 或 `ROLE: relay-lead` 時使用 `atlas-relay`；收到 `ROLE: worker` task package 時使用 `atlas-worker`。
- `codebase-atlas` 只在使用者明確要求建立、refresh、rebuild 或修復工程地圖時使用，不在普通開發中自動重建。

## 專案文件

- 根目錄優先保持少量入口文件：`AGENTS.md`、`README.md`、`DEVELOPMENT.md`；有 UI / design system 時再使用 `DESIGN.md`。
- 其他長期文件放 `docs/`。只有跨模組 runtime、state、外部系統或部署關係確實需要獨立高階視圖時才建立 `docs/architecture.md`；Atlas 已足夠的小型專案不要為了模板完整而建立。
- 工程地圖由 Codebase Atlas 維護；工作計畫與完成紀錄放 `docs/changes/`。不要把同一份事實重複寫進多個文件。
